import { style } from '@vanilla-extract/css';

export const header = style({
  margin: '16px',
  display: 'flex',
  alignItems: 'center',
});

export const title = style({
  fontSize: 'large',
  paddingLeft: '16px',
});
